{% extends 'base.html.twig' %}

{% block body %}

<main class="container">
    <section class="row">
        <div class="col-12">

            {% for posts in post %}

            {{ posts.title }}
            {{ posts.description }}
            {{ posts.content }}
            {{ posts.mediaUrl }}


            {% endfor %}
        </div>
    </section>
</main>

{% endblock %} 